export type PhysicsConcept = {
  title: string;
  description: string;
  formula: string;
  field: string;
  additionalInfo: string;
};

export interface ShareButtonProps {
  title: string;
  description: string;
}
